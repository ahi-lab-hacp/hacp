# Changesets

Run `pnpm changeset` for any user-visible change to a published package. Describe behavior and security impact clearly. Documentation-only and internal test changes do not require a changeset.
